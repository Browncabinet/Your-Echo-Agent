## Glama builder log — warning triage

Build finished green: `ESM ⚡️ Build success in 353ms` and `yourechoagent-mcp v0.4.0 ready on stdio`, tools list served over SSE. None of the red-tinted lines broke the build. Here's what each one is and whether to touch it.

### 1. `npm warn EBADENGINE pipenet@1.4.2 requires node >=22` — **do not fix**
`pipenet` is a transitive dep of `mcp-proxy@6.4.3`, which **Glama installs globally in their own Dockerfile** (`npm install -g mcp-proxy@6.4.3 pnpm@10.14.0` on Node 20). It's not in our `mcp-server/package.json`. mcp-proxy ran fine anyway (`[mcp-proxy] establishing new SSE stream…` succeeded). We can't change Glama's base image; nothing to do on our side.

### 2. `npm warn deprecated koa-router@14.0.0` — **do not fix**
Same source: transitive dep of Glama's globally-installed `mcp-proxy`. Cosmetic deprecation notice, not ours.

### 3. `warning: /root/.local/bin is not on your PATH` — **do not fix**
Emitted by the `uv` installer during Glama's Dockerfile. uv itself is symlinked into `/usr/local/bin` in the same RUN step, so the warning is moot. Not our Dockerfile.

### 4. `npm notice New major version of npm available` — **ignore**
Informational only.

### 5. `1 low severity vulnerability` after `mcp-server` install — **optional, verify only**
This one **is** in our tree (`mcp-server/package.json`). Action: read `mcp-server/package.json` + lockfile, run `cd mcp-server && npm audit --json` locally to identify the advisory, and if it's a low-severity transitive with a safe bump, apply `npm audit fix` (no breaking change) and commit the lockfile. If the only fix is `--force` / a major bump of a direct dep, leave it alone and document in the plan (low severity, not worth a breaking upgrade).

No changes needed to Supabase functions, marketing site, or the hosted `mcp-http` endpoint.

### Files that may change
- `mcp-server/package.json` (only if audit fix bumps a direct dep patch/minor)
- `mcp-server/package-lock.json` (regenerated by `npm audit fix`)

### Verify
- `cd mcp-server && npm audit` → confirm 0 vulnerabilities (or documented residual)
- `cd mcp-server && npm run build` → still succeeds
- No need to redeploy `mcp-http` or re-sync Glama (this is the stdio/npm mirror only)

### Non-goals
- No Dockerfile changes on our side
- No tool schema changes
- No Glama re-sync required for this pass
