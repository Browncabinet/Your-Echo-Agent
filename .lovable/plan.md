Remove the "Starting at $0/mo — View plans →" link from `src/pages/Auth.tsx` (currently line 178). The Auth page already renders the full `HomePricingSection` below the hero, so the "$0" teaser is redundant and misleading now that weekly plans start at $19.